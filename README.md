# Personal homepage

Live at **https://superwhu919.github.io**

Plain HTML and CSS. No build step. Edit `index.html`, then:

```sh
git add -A && git commit -m "Update" && git push
```

Live within a minute.

**Keep the resume gitignored.** GitHub Pages serves every committed file by
URL, so committing it would make it public even though nothing links to it.

## To do

- [ ] arXiv link for ShallowFeat, the last `href="#"` on the page
- [ ] Absolute `og:image` URL once there's a domain
